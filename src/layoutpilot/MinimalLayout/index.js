import { Outlet } from 'react-router-dom';
import '../../assets/css/stylepilot.css';

// ==============================|| MINIMAL LAYOUT ||============================== //

const MinimalLayout = () => (
  <>
    <Outlet />
  </>
);

export default MinimalLayout;
